syntax = "proto3";
package pb;

option go_package = "./pb;pb";

enum Grp {
  Def = 0;
  Sys = 1;
  Public = 2;
  Private = 3;
}

enum CmdSys {
  DefCmdSys = 0;
  KickOnlineUser = 1;
}

enum CmdPublic {
  DefCmdPublic = 0;
  HealthCheck = 1;
}

enum CmdPrivate {
  DefCmdPrivate = 0;
  Health = 1;
}


enum TypeKickReason {
  DefKickReason = 0;
  // 只能一个端登录
  OnlyOneClient = 1;
}

enum TypeTickerSubscribe {
  DefTickerSubscribe = 0;
  Test = 1;
}